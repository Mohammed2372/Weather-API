services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - REDIS_HOST=redis  # This maps to the service name below
      - REDIS_PORT=6379
    depends_on:
      - redis

  redis:
    image: redis:alpine